# DSH 0.1.7-rc.1 support — Requirements

**Date**: 2026-09-24

The user's request authorizes this feature round.

- R1: DSHmux must embed the 0.1.7-rc.1 Web UI, including relative plugin preloads, boot entries and batches, and both favicons, while preserving prior absolute URL support.
- R2: The maintained Harness branch must start at the upstream 0.1.7-rc.1 tag and provide JPEG model-request images when WebP would reach an endpoint.
- R3: Doctor must select the patched 0.1.7-rc.1 source by default and retain an official package choice. The checkout must be versioned and revision-pinned.
- R4: Tests and documentation must identify the verified version, branch, and limitations.
